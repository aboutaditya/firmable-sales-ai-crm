{
  "info": {
    "name": "Sales Intelligence API",
    "description": "Collection for the Sales Intelligence FastAPI service. Local demo mode works with an empty access token when AUTH_REQUIRED=false. Queue and Admin requests require a valid Supabase-compatible JWT in the accessToken collection variable.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:8000/api/v1" },
    { "key": "companyId", "value": "acme.com" },
    { "key": "accessToken", "value": "" },
    { "key": "cursor", "value": "" },
    { "key": "assignedUserId", "value": "sales-user-1" },
    { "key": "targetUserId", "value": "sales-user-2" },
    { "key": "teamId", "value": "team-a" }
  ],
  "auth": {
    "type": "bearer",
    "bearer": [
      { "key": "token", "value": "{{accessToken}}", "type": "string" }
    ]
  },
  "event": [
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "pm.test('Request completed', function () { pm.expect(pm.response.code).to.be.below(500); });",
          "if (pm.response.code === 200) { try { const body = pm.response.json(); if (body.next_cursor) pm.collectionVariables.set('cursor', body.next_cursor); } catch (e) {} }"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "System",
      "item": [
        {
          "name": "Health",
          "request": { "method": "GET", "header": [], "url": "{{baseUrl}}/health" }
        },
        {
          "name": "Liveness",
          "request": { "method": "GET", "header": [], "url": "{{baseUrl}}/health/live" }
        },
        {
          "name": "Readiness",
          "request": { "method": "GET", "header": [], "url": "{{baseUrl}}/health/ready" }
        }
      ]
    },
    {
      "name": "Queue",
      "item": [
        {
          "name": "Get my preferences",
          "request": { "method": "GET", "header": [], "url": "{{baseUrl}}/me/preferences" }
        },
        {
          "name": "Update my preferences",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"min_exposure_score\": 80\n}" },
            "url": "{{baseUrl}}/me/preferences"
          }
        },
        {
          "name": "Next lead",
          "request": { "method": "GET", "header": [], "url": "{{baseUrl}}/me/queue/next" }
        },
        {
          "name": "List my queue",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/me/queue?page=1&page_size=10",
              "host": ["{{baseUrl}}"],
              "path": ["me", "queue"],
              "query": [
                { "key": "page", "value": "1" },
                { "key": "page_size", "value": "10" }
              ]
            }
          }
        },
        {
          "name": "Update disposition",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"disposition\": \"qualified\",\n  \"notes\": \"Strong fit; spoke with the CTO.\",\n  \"next_follow_up_at\": \"2026-09-20T09:00:00Z\"\n}"
            },
            "url": "{{baseUrl}}/me/queue/{{companyId}}/disposition"
          }
        },
        {
          "name": "Record call activity",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"outcome\": \"connected\",\n  \"notes\": \"Reached the CTO; will follow up.\",\n  \"next_follow_up_at\": \"2026-09-20T09:00:00Z\"\n}"
            },
            "url": "{{baseUrl}}/me/queue/{{companyId}}/calls"
          }
        }
      ]
    },
    {
      "name": "Admin",
      "item": [
        {
          "name": "List users",
          "request": { "method": "GET", "header": [], "url": "{{baseUrl}}/admin/users" }
        },
        {
          "name": "Update user preferences",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"min_exposure_score\": 60\n}" },
            "url": "{{baseUrl}}/users/{{targetUserId}}/queue-preferences"
          }
        }
      ]
    },
    {
      "name": "Companies",
      "item": [
        {
          "name": "List companies",
          "request": {
            "method": "GET",
            "header": [],
            "url": { "raw": "{{baseUrl}}/companies?limit=20&offset=0", "host": ["{{baseUrl}}"], "path": ["companies"], "query": [{ "key": "limit", "value": "20" }, { "key": "offset", "value": "0" }] }
          }
        },
        {
          "name": "List companies - filters",
          "request": {
            "method": "GET",
            "header": [],
            "url": { "raw": "{{baseUrl}}/companies?country=US&min_score=40&industry=technology&min_employee_count=100&signal=critical&signal=rdp&limit=20", "host": ["{{baseUrl}}"], "path": ["companies"], "query": [
              { "key": "country", "value": "US" }, { "key": "min_score", "value": "40" }, { "key": "industry", "value": "technology" }, { "key": "min_employee_count", "value": "100" }, { "key": "signal", "value": "critical" }, { "key": "signal", "value": "rdp" }, { "key": "limit", "value": "20" }
            ] }
          }
        },
        {
          "name": "List companies - next cursor",
          "request": {
            "method": "GET",
            "header": [],
            "url": { "raw": "{{baseUrl}}/companies?limit=20&cursor={{cursor}}", "host": ["{{baseUrl}}"], "path": ["companies"], "query": [{ "key": "limit", "value": "20" }, { "key": "cursor", "value": "{{cursor}}" }] }
          }
        },
        {
          "name": "Get company",
          "request": { "method": "GET", "header": [], "url": "{{baseUrl}}/companies/{{companyId}}" }
        },
        {
          "name": "Assign company",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"user_id\": \"{{assignedUserId}}\",\n  \"team_id\": \"{{teamId}}\"\n}" },
            "url": "{{baseUrl}}/companies/{{companyId}}/assign"
          }
        }
      ]
    },
    {
      "name": "AI",
      "item": [
        {
          "name": "Assess company",
          "request": { "method": "POST", "header": [], "url": "{{baseUrl}}/companies/{{companyId}}/assess" }
        },
        {
          "name": "Generate company summary",
          "request": { "method": "POST", "header": [], "url": "{{baseUrl}}/companies/{{companyId}}/summary" }
        },
        {
          "name": "Generate outreach draft",
          "request": { "method": "POST", "header": [], "url": "{{baseUrl}}/companies/{{companyId}}/outreach" }
        }
      ]
    }
  ]
}