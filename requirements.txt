# Runtime dependencies (Vercel-optimized: <225 MB)
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
psycopg[binary]>=3.2,<4.0
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
PyJWT>=2.8,<3.0

# Optional: only needed if using Parquet/DuckDB analytics locally
# duckdb>=1.0,<2.0
# pyarrow>=15.0,<26.0
# zstandard>=0.22,<1.0
